name: Apply Bjerke live source-gap hotfix

on:
  push:
    branches:
      - fix/official-nordic-identity-gaps-v109-final
    paths:
      - scripts/apply-v110-hotfix.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply and validate focused hotfix
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/apply-v110-hotfix.py
          npm test
          rm -f scripts/apply-v110-hotfix.py .tmp/v109.patch.gz.b64 .github/workflows/apply-v109-transfer.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Hotfix Nordic live source-gap normalization"
          git push origin HEAD:fix/official-nordic-identity-gaps-v109-final
