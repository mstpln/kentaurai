name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Aligned tests
        run: node --test test/aligned*.test.js

      - name: Analysis tests
        run: node --test test/analysis*.test.js

      - name: Tests b-d
        run: |
          shopt -s nullglob
          files=(test/[b-d]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests e-g
        run: |
          shopt -s nullglob
          files=(test/[e-g]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests h-r
        run: |
          shopt -s nullglob
          files=(test/[h-r]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests sa-f
        run: |
          shopt -s nullglob
          files=(test/s[a-f]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests sg-l
        run: |
          shopt -s nullglob
          files=(test/s[g-l]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests sm-r
        run: |
          shopt -s nullglob
          files=(test/s[m-r]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests ss-z
        run: |
          shopt -s nullglob
          files=(test/s[s-z]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Tests t-z
        run: |
          shopt -s nullglob
          files=(test/[t-z]*.test.js)
          if ((${#files[@]})); then node --test "${files[@]}"; fi

      - name: Syntax - index
        run: node --check src/index.js

      - name: Syntax - worker settings
        run: node --check src/worker-settings.js

      - name: Syntax - settings data
        run: node --check src/settings-data.js

      - name: Syntax - app page settings
        run: node --check src/app-page-settings.js
